package ir.sudoit.infrastructure.crud.controller;


/**
 * Marker interface that specifies validation groups and have to be used as 'group' parameter of constraints
 * in the input (request) DTOs, and as parameter of {@code @Validated} annotation in "update" controller methods.
 */
public interface OnUpdate {
}